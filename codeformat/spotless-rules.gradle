spotless {
    java {
        endWithNewline()
        indentWithSpaces()
        trimTrailingWhitespace()
        removeUnusedImports()
        toggleOffOn()
        eclipse().configFile rootProject.file('codeformat/formatter-config.xml')
    }
}
